{
  "name": "voice-listener",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "./start.sh",
    "extract": "bun run src/index.ts",
    "execute": "bun run src/action-executor.ts",
    "logwatch": "bun run src/log-watcher.ts",
    "dev:extract": "bun --watch src/index.ts",
    "dev:execute": "bun --watch src/action-executor.ts",
    "dev:logwatch": "bun --watch src/log-watcher.ts"
  },
  "dependencies": {
    "@instantdb/admin": "^0.18.7"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
